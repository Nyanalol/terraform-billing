# Valores reales por país — NUNCA subir al repositorio
terraform.tfvars
tfvars/*.tfvars
!tfvars/example.tfvars

# Logs locales
logs/

# Estado y caché de Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# Credenciales y claves
*.json
*.pem
*.key
